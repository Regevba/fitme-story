# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Sync script cache
.showcase-cache/
.sync-report.md

# unified-control-center sync artifacts (Pattern 4.b)
# Regenerated by `npm run prebuild` on every build (local + Vercel).
# Source of truth lives in FitTracker2 repo at .claude/shared/ + .claude/features/
src/data/shared/
src/data/features/
src/data/freshness.json
